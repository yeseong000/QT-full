# 인수인계 — STEP2 '임베딩 관문 + 가변 개수' 구현·배포 (2026-08-03)

STEP2 '떠오르는 질문' 중복을 **객관적 잣대(답변 임베딩)** 기준으로 잡고, 관주 배경지식형을 얹어 배포하는 작업. 이번 세션에서 무엇을 밝혔고, 무엇이 이미 됐고, 새 세션이 무엇을 구현·배포하면 되는지.

> **말투/원칙:** 사용자는 비전공 바이브 코더 — 쉬운 비유·정중한 말투. **git push는 명시 승인 전까지 금지**(commit만). API 키 포함 파일 커밋 금지. 실제 파이프라인은 승인 전 안 건드림.

---

## 0. 30초 요약

**목표:** STEP2 9개 질문의 중복을 **"답변 임베딩이 겹치는 질문만 빼는 관문(embedding gate) + 가변 개수"**로 해결하고, 관주 배경지식형(메인3)을 얹어 배포한다.

**이번 세션의 대반전:** "중복 35%"는 대부분 **LLM 판정 잣대의 착시**였다. 프로젝트 객관 잣대(`_measure_answer_overlap.py`, 답변 임베딩 코사인 ≥0.75)로 재니 **9일 중 7일이 9개에서 이미 깨끗**했다. 진짜 겹침은 **극단적 단일주제 본문**(예: 압살롬 도망)에서만 난다.

**현황:**
- ✅ **§7 관주 배경지식형 + §8 관주 메인3 소프트예약 + 추상질문 억제** = 구현·검증 완료. 브랜치 `gwanju-main3-exp`(미배포).
- ⚠️ **임베딩 관문 + 가변 개수** = 측정 도구(`_exp_gen_embed.py`)로 **개념 검증만** 완료. **실제 엔진(`run_simple`)엔 미구현.**

**다음 3단계(사용자 확정):**
1. **① 임베딩 관문을 `run_simple`에 실제 구현** + 프론트가 가변 개수를 받는지 확인 (격리에서).
2. **② 진짜 파이프라인 경로로 7/26~8/02를 재생성**해 결과(질문·개수·관주)를 **사용자에게 먼저 보여줌.**
3. **③ 사용자가 "좋다, 올려라" 하면** → 백업 후 재생성 + commit·push(배포). 내일 자동생성부터 새 엔진.

**⚠️ 배포 = 큰 릴리스다.** origin/main은 **아직 옛 엔진**(§8 참고). 새 엔진 자체가 미배포 WIP였다.

---

## 1. 왜 이 조사를 했나 (맥락)

사용자 요청 흐름: "STEP2 중복이 많다 → 관주(메인3)로 중복을 대체하자". 이걸 실측하며 근본 원인을 팠다. 관련 선행: `인수인계_관주_배경지식형_0730.md`(§7·§8 설계), `인수인계_STEP2새코드_검증결과_0729.md`(임베딩 처방).

이 세션이 만든 리포트 2개(로컬, 참고용):
- `질문떠올리기_중복분석_7월_0730.html` — 7월 중복 캘린더(옛 LLM 잣대 기반, **지금은 과대평가로 판명**)
- `질문개수_연구_왜9개는중복인가_0801.html` — "본문당 ~7개, 9개 강요가 원인" 직관 설명(연못/낚싯대 비유)

---

## 2. 【핵심 반전】 잣대가 문제였다

중복 판정을 **LLM(gpt-4o)에게 시키면 결과가 크게 흔들린다** — 7/31에서 감지기A는 "2건", 채점기B는 "6건(85.7%)". "여인의 청원"을 소재 1개로 볼지 7개로 볼지가 주관적이라서다.

**프로젝트 객관 잣대 = `scripts/_measure_answer_overlap.py`** (07-17·19 조사에서 확립):
- **답변을 임베딩(text-embedding-3-small)해 코사인 ≥ 0.75면 '겹침 의심'.** 어휘 바꿔도 안 뚫린다.
- deterministic + `_embed_cache.json` 캐시 → 재실행 무료.
- ⚠️ 단 "선별 그물이지 판결 아님" — 보정 결과 진짜겹침(0.76)이 오탐(0.79)보다 낮은 구간 존재. 완벽한 경계선 없음. 걸린 건 사람이 확인.

**같은 3일, 두 잣대 비교:**

| 날 | LLM 판정(옛) | 답변 임베딩(객관) |
|---|---|---|
| 7/31 | 85.7% ❌ | **겹침 0건 ✅** |
| 8/01 | 66.7% ❌ | **겹침 0건 ✅** |
| 7/30 | 0% | **진짜 겹침**(압살롬 도망 답변 0.75~0.80 수렴) |

→ 내가 앞서 보고한 "평균 35% 중복"도 **부풀려진 값**이었다. **판정은 오직 임베딩으로.**

---

## 3. 검증 결과 (9일, 객관적 잣대)

`run_simple`(현 gwanju-main3-exp 엔진)로 답변까지 생성 → `_measure_answer_overlap` 적용:

| 날 | 본문 | 본따 | 9개 중 진짜 겹침 | 정직한 개수 | 관주 |
|---|---|---|---|---|---|
| 7/10 | 삼하 5:1-5 | 2 | 0 | 9 | ✅ |
| 7/11 | 5:6-12 | 1 | 0 | 9 | ✅ |
| 7/12 | 5:13-25 | 2 | 0 | 9 | ✅ |
| 7/13 | 6:1-11 | 2 | 0 | 9 | ✅ |
| 7/14 | 6:12-23 | 2 | 1 | 8 | — |
| 7/15 | 7:1-17 | 1 | 0 | 9 | ✅ |
| 7/30 | 13:30-39 | 1 | 다수(수렴) | **5** | ✅ |
| 7/31 | 14:1-11 | 1 | 0 | 9 | ✅ |
| 8/01 | 14:12-20 | 1 | 0 | 9 | ✅(4개) |

**패턴 확정:** 9일 중 **7일이 9개에서 이미 깨끗**, 7/14→8, 압살롬 도망(7/30)만 →5. **관주 8/9일 등장, 전부 구절 지목**(삼하 23:39, 열왕기하 6:26-28, 열왕기상 3:9, 누가 18:3…).
결론: 엔진은 이미 대체로 잘 한다. 필요한 건 **드물게 겹치는 날만 자동으로 빼주는 관문**이다.

---

## 4. 이미 구현·검증된 것 (브랜치 `gwanju-main3-exp`)

**위치: 로컬 클론 `Documents/Playground/qt-push-tmp/`, 브랜치 `gwanju-main3-exp`(커밋 af2b68a).**
이 브랜치 = 새 엔진 WIP(`wip-followup-engine-0731`) + 아래 수정.

### ① §7 관주 배경지식형 — `scripts/followup_simple.py` `_Q_WRAPPER` 관주형 규칙
- "다른 성경과 어떻게 연결?" 뭉개기 → **낯선 인물의 정체·본문이 인용한 옛 사건·행동/판결의 근거가 된 율법·관습**을 콕 집는 배경지식형으로. 뭉개기 ✗예시 + 정답 few-shot(우리아=삼하23:39, 네배=출22:1).

### ② §8 관주 메인3 소프트예약 — `scripts/followup_simple.py` `_assemble_diverse`
- 메인 3개 뽑기 전, **검증된 관주형(anchor_type=="관주", anchor_ok) 1개를 메인3에 먼저 배치**, 나머지 2개는 공유 카운터로. 관주 후보 없으면 자동 해제(억지로 안 만듦). 검증: 관주-메인3 1/6 → 6/6.

### ③ 추상질문 억제 — `prompts/follow_up/parts/03_question_rules.md`
- 기존 "'왜·이유·의미'는 금지 아님" 섹션을 **뒤집음**: 주인공(다윗·압살롬 등)의 **중심 행동에 대한 '왜/이유/배경/결과/영향/의미'는 만들지 마라**(답이 오늘 사건 하나로 수렴). 대신 서로 다른 **구체적 대상**(조연 정체·지명·사물·숫자·관습·원어·관주)을 콕. '왜'는 낯선 주변 배경 열 때만 허용.
- ⚠️ 단독으로는 효과 약함(좁은 본문은 여전히 이유질문). **임베딩 관문이 실질 해결책**이다.

측정 도구도 이 브랜치에 보존: `scripts/_exp_gen_embed.py`(객관 잣대 측정, 핵심), `_exp_gen_variable.py`, `_exp_analyze_dup_exp.py` 등. ⚠️ 안에 **스크래치패드 절대경로가 하드코딩**돼 있으니 새 환경에 맞게 EXP 경로만 고쳐 재사용.

---

## 5. 【만들 것】 임베딩 관문 + 가변 개수 — `run_simple`에 구현

**개념:** 9개 답변을 만든 뒤, **답변 임베딩이 ≥0.75로 겹치는 쌍은 하나를 버려** distinct만 남긴다(보통 9, 가끔 5~8). 07-29가 처방한 바로 그것.

**구현 지점:** `followup_simple.py` `run_simple` — ④ 답변 생성(`fp.write_answers`, 현 ~1072행) **직후**. 지금은 답변 쓰고 바로 items 조립. 여기에:
1. 확정 9개 **답변**을 `_measure_answer_overlap.embed_all()`로 임베딩(같은 모델·캐시 재사용).
2. 쌍별 코사인, **≥0.75 쌍을 union-find로 묶어** 그룹당 1개만 유지(관주·안전군 우선 유지 권장).
3. 살아남은 것만 `items`로. **꼬리는 부모 메인이 살아있을 때만.** 구조(메인+꼬리 중첩)는 유지.
4. `_followup_meta`에 `dropped_by_embed`, `final_count` 기록(관측용).

**참고:** `_exp_gen_embed.py`가 이 로직을 하네스로 이미 구현해 뒀다 — 그대로 `run_simple` 안으로 옮기면 된다. `embed_all`/`cosine`/`SIM_THRESHOLD` 재사용.

**프론트 확인 (필수):** `src/qt/step-2-meditation.html`의 `renderFollowUp(items)`는 `items.map(...)`으로 메인 개수만큼 pill을 그리고, 꼬리도 `item.follow_ups` 개수만큼 그린다 → **가변 개수를 이미 받을 가능성이 높다.** 그래도 **메인 2개/1개, 꼬리 0개인 케이스를 실제로 렌더해 깨지는지 반드시 눈으로 확인**(Playwright 캡처). 캐러셀·도트·번호 표기 주의.

---

## 6. 【배포 절차 ④⑤】 — 사용자 승인 후에만

> ⚠️ **origin/main은 아직 옛 엔진(18후보, 관주 없음).** 새 엔진 전체가 미배포다. 배포 = 아래를 한꺼번에 origin/main에 올리는 **큰 릴리스**:
> `scripts/followup_simple.py`(새 엔진+§7·§8+관문), `scripts/generate_meditation.py`(fs.run_simple 호출본), `scripts/openbible_xref.py`, 프롬프트 파트들. `wip-followup-engine-0731`/`gwanju-main3-exp`에 다 있다.

**절차(승인 후):**
1. **백업:** 7/26~8/02의 기존 `data/deep_dive/*.json`를 `.prefix.bak`로 복사(도구 참고: 옛 인수인계의 `_regen_followup_date.py`가 .bak 생성).
2. **재생성:** 새 엔진으로 7/26~8/02 `data/{deep_dive,ai,qt}` 재생성(진짜 파이프라인 경로 = `generate_meditation.py --passage`).
3. **로컬 확인** → 사용자에게 결과 보여줌 → OK.
4. **commit + push** origin/main. **sw.js CACHE 버전 bump 필수**(프론트 건드렸으면 — 안 하면 기기 반영 안 됨).
5. 내일 새벽 `daily_qt.yml`이 새 엔진으로 자동생성.

---

## 7. 코드·자산 지도

| 자산 | 위치 |
|---|---|
| **작업 클론(최신)** | `Documents/Playground/qt-push-tmp/` — 첫 작업 `git fetch; git checkout gwanju-main3-exp` |
| 새 엔진 WIP(베이스) | 브랜치 `wip-followup-engine-0731`(커밋 c021ace) |
| §7·§8·추상억제+도구 | 브랜치 `gwanju-main3-exp`(커밋 af2b68a) ← **여기서 시작** |
| 객관 잣대 | `scripts/_measure_answer_overlap.py`(≥0.75) |
| 측정 하네스 | `scripts/_exp_gen_embed.py`(핵심), `_exp_*` (경로 하드코딩 주의) |
| Desktop 클론(사용자 열람용) | `Desktop/앱 개발/7.주만나 큐티/` — **48커밋 뒤처짐 + 리디자인 WIP, 여기서 작업 금지** |
| 리포트 2개 | Desktop 클론 root의 `질문떠올리기_중복분석_7월_0730.html`, `질문개수_연구_왜9개는중복인가_0801.html` |

---

## 8. 함정 (미리 알고 갈 것)

1. **origin/main = 옛 18후보 엔진(관주 없음).** 배포는 새 엔진 전체를 올리는 큰 릴리스다. 07-29엔 "배포 말자" 판정 받았던 코드 — 단 그 판정은 흔들리는 LLM 잣대 탓이 컸고, **객관 잣대론 훨씬 낫다**(§3).
2. **중복 잣대는 오직 답변 임베딩(≥0.75).** LLM 판정·distinct/topic 지표는 가짜다(2↔6 흔들림 실측).
3. **0.75는 선별망이지 판결 아님.** 오탐 있음. 관문은 "과잉 신고 후 안전군·관주 우선 유지"로 보수적으로.
4. **개수 가변 → 프론트 필수 확인.** 메인 1~2개, 꼬리 0개 케이스 실렌더 검증.
5. **좁은 단일주제 본문**(압살롬 도망류)은 정직하게 5개까지 준다 — 억지로 9 채우지 말 것.
6. **관주 과다 주의:** 8/01은 관주 4개 나옴. "절반 넘기지 마라"를 관문/선택에서 상한으로 강제 검토.
7. push 전 **sw.js CACHE bump**, **API 키 커밋 금지**.
8. 측정 도구의 **/c/ msys 경로는 파이썬이 못 읽음** — Windows 경로(C:/…)로.

---

## 9. 현재 상태 / 이 세션이 남긴 것 (2026-08-03)

- **전부 격리 실험.** 실제 파이프라인·사용자 데이터 안 건드림. 사용자 리디자인 WIP도 `wip-followup-engine-0731`에 안전 저장.
- **미배포·미푸시.** §7·§8·추상억제+도구는 `gwanju-main3-exp`(af2b68a)에 커밋됨.
- 누적 실험 API 비용 약 5,500원.
- **다음 세션 첫 작업:** `cd Documents/Playground/qt-push-tmp && git checkout gwanju-main3-exp` → §5(임베딩 관문 구현) → §6 절차로 사용자 승인받아 배포.

관련 기억: `project_jumanna_step2_july_dupreport`, `project_jumanna_gwanju_background`, `feedback_push_control`, `feedback_api_key_security`.
